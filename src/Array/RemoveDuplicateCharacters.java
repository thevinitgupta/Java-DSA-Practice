package Array;

import java.util.*;

public class RemoveDuplicateCharacters {

    //[a,v,a,b,c,d,v,a,d] => [b,c,v,a,d]
    public static char[] removeDuplicates(char[] arg){
        ArrayList<Character> arr = new ArrayList<>();
        HashMap<Character,Integer> map = new HashMap<>();
        int n = arg.length;
        int k=0;
        return arg;
    }
}
